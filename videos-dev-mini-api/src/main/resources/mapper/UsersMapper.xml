<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lidsu.mapper.UsersMapper">
  <resultMap id="BaseResultMap" type="com.lidsu.pojo.Users">
    <!--
      WARNING - @mbg.generated
    -->
    <result column="id" jdbcType="CHAR" property="id" />
    <result column="username" jdbcType="CHAR" property="username" />
    <result column="password" jdbcType="CHAR" property="password" />
    <result column="face_image" jdbcType="CHAR" property="faceImage" />
    <result column="nickname" jdbcType="CHAR" property="nickname" />
    <result column="fans_counts" jdbcType="BIGINT" property="fansCounts" />
    <result column="follow_counts" jdbcType="BIGINT" property="followCounts" />
    <result column="receive_like_counts" jdbcType="BIGINT" property="receiveLikeCounts" />
  </resultMap>

  <update id="addReceiveLikeCount" parameterType="String">
        update users set receive_like_counts=receive_like_counts+1
        where id=#{userId}
  </update>
  <update id="reduceReceiveLikeCount" parameterType="String">
    update users set receive_like_counts=receive_like_counts-1
    where id=#{userId}
  </update>


  <update id="addFansCount" parameterType="String">
    update users set fans_counts=fans_counts+1
    where id=#{userId}
  </update>
  <update id="addFollersCount" parameterType="String">
    update users set follow_counts=follow_counts+1
    where id=#{userId}
  </update>
  <update id="reduceFansCount" parameterType="String">
    update users set fans_counts=fans_counts-1
    where id=#{userId}
  </update>
  <update id="reduceFollersCount" parameterType="String">
    update users set follow_counts=follow_counts-1
    where id=#{userId}
  </update>



</mapper>